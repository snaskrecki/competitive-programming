#include <bits/stdc++.h>

#define FOR(x, b, e) \
    for (int x = b; x <= (int)(e); ++x)
#define FORD(x, b, e) \
    for (int x = b; x >= (int)(e); --x)
#define FORS(x, b, e, s) \
    for (int x = b; x <= (int)(e); x+=s)
#define FORDS(x, b, e, s) \
    for (int x = b; x >= (int)(e); x-=s)
#define REP(x, n) \
    for (int x = 0; x < (int)(e); ++x)
#define VAR(v, n) \
    decltype(n) v = (n)
#define ALL(c) \
    (c).begin(), (c).end()
#define SIZE(x) \
    ((int)(x).size())
#define FOREACH(i, c) \
    for (VAR(i, (c).begin()); i != (c).end(); ++i)
#define PB push_back
#define MP make_pair
#define ST first
#define ND second

using namespace std;

const int INF = 1e9 + 1;
const double EPS = 10e-9;

using VI = vector<int>;
using LL = long long;
using VVI = vector<VI>;
using VL = vector<LL>;
using VD = vector<double>;
using VS = vector<string>;
using PII = pair<int, int>;
using VPII = vector<PII>;

int main() {
    std::ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    cout << "Hello, World\n";

    return 0;
}
